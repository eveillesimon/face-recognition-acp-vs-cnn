This projects compares 2 face classification methods : CNN (convolutional neural network) and a KNN (k-nearest neighbourghs) combined with a PCA (principal component analysis).

Have a look at the notebook to see the result.
